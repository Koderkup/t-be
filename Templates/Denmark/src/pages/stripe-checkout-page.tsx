import { FC } from 'react';
import { StripeCheckoutPageLayout } from '@/components';

const StripeCheckoutPage: FC = () => {
  return <StripeCheckoutPageLayout />;
};

export default StripeCheckoutPage;
